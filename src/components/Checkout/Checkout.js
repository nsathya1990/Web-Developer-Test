import React from 'react';

import classes from './Checkout.module.scss';

const Checkout = () => (
  <div>
    <h2>Checkout</h2>
    <p>
      Enter each item on a new line, choose the amount of groups unders
      settings, and click the button to generate your randomized list. Don't
      like the first team? Just click again until you do.
    </p>
  </div>
);

export default Checkout;
